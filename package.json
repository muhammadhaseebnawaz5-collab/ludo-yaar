{
  "name": "ludo-multiplayer-root",
  "version": "1.0.0",
  "description": "Ludo Game Root Package",
  "scripts": {
    "install": "cd website && npm install && cd ../server && npm install",
    "build": "cd website && npm run build",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
